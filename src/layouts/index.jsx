import React from 'react'

const IndexLayout = () => <div>Built</div>

IndexLayout.propTypes = {
  // eslint-disable-next-line react/forbid-prop-types
}

export const query = null

export default IndexLayout
